{% import "_helpers/images" as images %}
{% set appLinks = appLinksEntry.all() %}
{% for link in appLinks %}
	<a class="btn btn-secondary px-3 py-2 mt-3 me-2" href="{{link.linkUrl}}">
		{{ images.singleImg(link.image.0, 90, {ratio: 3.6}) }}
	</a>
{% endfor %}
