{{block.type.handle }}
{% set selectEntries = block.entriesList %}
{% if selectEntries is defined and selectEntries|length %}
	{% for entryData in selectEntries %}
            {% set handlerName = entryData.type.handle %}
            {% include ["_includes/matrix-blocks/type/" ~ handlerName] with { block: entryData } %}
	{% endfor %}
{% endif %}
